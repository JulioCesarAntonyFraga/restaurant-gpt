name: Placeholder CI

on:
  pull_request:
    branches:
      - master

jobs:
  always-pass:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Placeholder de build
        run: echo "Tudo certo!"
